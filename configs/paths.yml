# ==============================================================================
# Branch 2 Directory Paths (canonical)
# ==============================================================================

# RAW DATA — ERA5 GRIB files + metadata
raw_dir: data/raw/era5
metadata_dir: data/metadata

# INTERMEDIATE — GRIB → Parquet conversion outputs (Stage 2)
intermediate_dir: data/intermediate

# LOGS — runtime logs
logs_dir: data/logs

# FEATURES — ML-ready features (Stage 4)
features_dir: data/features

# MODEL ARTIFACTS — trained model files (Stage 6)
model_artifact_dir: models

# PREDICTIONS — model outputs (Stage 7)
predictions_dir: data/predictions

# CONFIGS — Branch 2 config files (variables, years, months)
config_dir: configs

# ==============================================================================
# Stage 3 Chunk Outputs (Branch 2)
# ==============================================================================
chunk_output_dir: data/chunks

# Stage 3 Metadata (per-chunk JSON)
chunk_metadata_dir: data/chunks_metadata

# ==============================================================================
# Stage 3 Merge Outputs (Branch 2)
# ==============================================================================
stage3_merged: data/intermediate/merged.nc
stage3_metadata: data/intermediate/merged_metadata.json
stage3_qc: data/intermediate/merged_qc.json
