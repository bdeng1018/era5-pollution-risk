"""
ERA5 pipeline source package.

This initializer intentionally defines no imports or logic to avoid
double-import issues during `python -m` execution and to ensure pytest
can discover the package cleanly.
"""
